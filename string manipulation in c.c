#include<stdio.h>
#include<string.h>
int main (){
    char str1[100];char str2[100];char ch;
    print f ("enter the str1");
    scan f("%s",&str1);
    print f ("enter the str2");
    scan f("%s",&str2);
    print f ("enter the choice");
    scan f("%s",&ch);
    switch(ch)
    {
        case1:print f("length of str1:%/u\n"strlen(str1));
        case2:print f("after concatenation:%s/n",str1);
        strcpy(s2,s1);
        break;
        case3:print f("copied string:%s/n";s2);
        strcpy(s1,s2);
        break;
        case4:print f("invalid operator\n");
    }
    return 0;
}
